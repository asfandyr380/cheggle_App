import 'package:flutter/material.dart';

class ChooseBusiness extends StatefulWidget {
  ChooseBusiness({Key key}) : super(key: key);

  @override
  _ChooseBusinessState createState() {
    return _ChooseBusinessState();
  }
}

class _ChooseBusinessState extends State<ChooseBusiness> {
  @override
  void initState() {
    super.initState();
  }

  @override
  void dispose() {
    super.dispose();
  }

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.transparent,
      body: Container(
        color: Colors.red,
      ),
    );
  }
}
